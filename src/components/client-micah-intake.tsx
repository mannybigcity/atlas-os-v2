"use client";

import { useActionState } from "react";
import { formatDateTime } from "@/lib/format";
import { submitClientAiRequest } from "@/server/client-ai/actions";
import type { ClientAiRequest } from "@/server/client-ai/queries";
import { initialClientAiActionState, type ClientAiActionState } from "@/server/client-ai/types";

type ClientMicahIntakeProps = { organizationId: string; previewMode: boolean; recentRequests: ClientAiRequest[] };

function statusClass(status: ClientAiActionState["status"]) {
  if (status === "success") return "bg-emerald-100 text-emerald-800";
  if (status === "blocked") return "bg-amber-100 text-amber-800";
  if (status === "failed") return "bg-rose-100 text-rose-800";
  return "bg-slate-100 text-slate-700";
}

export function ClientMicahIntake({ organizationId, previewMode, recentRequests }: ClientMicahIntakeProps) {
  const [state, formAction, pending] = useActionState(submitClientAiRequest, initialClientAiActionState);
  return (
    <section className="rounded-[2rem] border border-violet-200 bg-[radial-gradient(circle_at_top_left,rgba(86,114,240,0.14),transparent_30%),linear-gradient(180deg,#ffffff_0%,#f8f7ff_100%)] p-5 shadow-[0_16px_45px_rgba(15,23,42,0.05)]">
      <div className="flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between"><div><p className="text-[11px] font-black uppercase tracking-[0.2em] text-[#5672f0]">Micah request</p><h2 className="mt-2 text-3xl font-semibold tracking-[-0.05em] text-slate-950">Tell Micah what you want to post</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Give Atlas a plain-English brief for social post ideas, caption drafts, campaign angles, or content prompts for this organization.</p></div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-blue-100 px-3 py-1 text-xs font-semibold uppercase tracking-[0.12em] text-blue-700">{recentRequests.length} recent</span><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold uppercase tracking-[0.12em] text-slate-700">{previewMode ? "Read only" : "Live request"}</span></div></div>
      <form action={formAction} className="mt-5 grid gap-4 xl:grid-cols-[1fr_0.78fr]"><input name="organizationId" type="hidden" value={organizationId} /><input name="role" type="hidden" value="micah" /><div className="rounded-[1.5rem] border border-slate-200 bg-white p-4"><label className="block"><span className="mb-2 block text-sm font-semibold text-slate-800">What should Micah create?</span><textarea className="min-h-36 w-full rounded-2xl border border-slate-300 bg-white px-4 py-3 text-sm leading-6 text-slate-950 outline-none transition placeholder:text-slate-400 focus:border-[#5672f0] focus:ring-4 focus:ring-[#dfe7ff]" disabled={previewMode || pending} name="prompt" placeholder="Promote our Labor Day sale. We need 3 social media post ideas about our new service this week." required /></label><div className="mt-4 flex flex-col gap-3 sm:flex-row sm:items-center"><button className="inline-flex items-center justify-center rounded-full bg-[#5672f0] px-5 py-3 text-sm font-semibold text-white transition hover:-translate-y-0.5 hover:bg-[#465fd1] disabled:cursor-not-allowed disabled:opacity-60" disabled={previewMode || pending} type="submit">{pending ? "Sending..." : "Send to Micah"}</button><p className="text-xs leading-5 text-slate-500">Keep it specific: event, offer, service, audience, or deadline.</p></div>{state.status !== "idle" ? <div className="mt-4 rounded-2xl border border-slate-200 bg-slate-50 p-4"><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-[0.12em] ${statusClass(state.status)}`}>{state.status}</span><span className="rounded-full bg-white px-3 py-1 text-xs font-semibold uppercase tracking-[0.12em] text-slate-600 ring-1 ring-slate-200">Micah</span></div>{state.error ? <p className="mt-3 text-sm leading-6 text-rose-700">{state.error}</p> : null}{state.answer ? <div className="mt-3 space-y-3"><p className="whitespace-pre-wrap text-sm leading-7 text-slate-700">{state.answer}</p>{state.nextStep ? <div className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-xs font-black uppercase tracking-[0.18em] text-slate-500">Next step</p><p className="mt-2 text-sm leading-6 text-slate-700">{state.nextStep}</p></div> : null}{state.createdAt ? <p className="text-xs font-medium text-slate-500">Logged {formatDateTime(state.createdAt)}</p> : null}</div> : null}</div> : null}</div><div className="rounded-[1.5rem] border border-slate-200 bg-white p-4"><p className="text-xs font-black uppercase tracking-[0.18em] text-slate-500">Recent Micah requests</p><div className="mt-4 space-y-3">{recentRequests.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-5 text-sm leading-6 text-slate-600">No Micah requests have been sent yet.</div> : null}{recentRequests.slice(0, 4).map((request) => <article className="rounded-2xl border border-slate-200 bg-slate-50 p-4" key={request.id}><p className="text-xs font-black uppercase tracking-[0.16em] text-[#5672f0]">{request.status.replaceAll("_", " ")}</p><p className="mt-2 text-sm leading-6 text-slate-700">{request.prompt}</p><p className="mt-2 text-xs font-medium text-slate-500">{formatDateTime(request.createdAt)}</p></article>)}</div></div></form>
    </section>
  );
}
